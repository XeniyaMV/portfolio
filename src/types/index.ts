export interface NavItem {
  title: string;
  url: string;
}

export interface ProjectCard {
  image: string;
  title: string;
  techStack: string[];
  description: string;
  linkToDeploy: string;
  linkToCode: string;
}
